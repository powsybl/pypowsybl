name: CI

on: [push]

jobs:
  build:
    name: Build ${{ matrix.config.name }} ${{ matrix.python.name }} wheel
    runs-on: ${{ matrix.config.os }}
    container: quay.io/pypa/manylinux2014_x86_64
    strategy:
      matrix:
        config:
          - {
            name: linux,
            os: ubuntu-latest,
          }
        python:
          - {
            name: cp37,
            version: '3.7',
          }

    steps:
      - name: Install Linux packages
        run: yum install maven zlib-devel.x86_64

      - name: Checkout sources
        uses: actions/checkout@v1
        with:
          submodules: true

      - name: Setup GraalVM
        uses: ayltai/setup-graalvm@v1
        with:
          java-version: 11
          graalvm-version: 20.3.0
          native-image: true

      - name: Build wheel
        run: /opt/python/cp37-cp37m/bin/python3 setup.py bdist_wheel

      - name: Install wheel
        shell: bash
        run: /opt/python/cp37-cp37m/bin/pip3 install dist/*.whl --user

      - name: Run tests
        run: /opt/python/cp37-cp37m/bin/python3 -m unittest tests/test.py

      - name: Upload wheel
        uses: actions/upload-artifact@v2
        with:
          name: gridpy-wheel-${{ matrix.config.name }}-${{ matrix.python.name }}
          path: dist/*.whl

  package:
    name: Package wheels
    runs-on: ubuntu-latest
    needs: build

    steps:
      - name: Download wheels
        uses: actions/download-artifact@v2
        with:
          path: download

      - name: Upload wheels
        uses: actions/upload-artifact@v2
        with:
          name: gridpy-wheels
          path: download/**/*.whl
